/**
 * A fast algorithm for counting the maximum number of
 * sequences available in the given cards array.
 * The input array is assumed to be sorted and of
 * a single Suit.
 */
const countSequences = (cards) => {
  for (let i = 0; i < cards.length; i += 1) {

  }
};

export default countSequences;
